@model globalPoetryRozygrysh.Models.RapLineModel
<div class="jumbotron" style="background: linear-gradient(to right, transparent, mistyrose), url(/Content/images/Zakaz.jpg); border: solid 2px cyan; position: relative;">
    <form class="" method="post" action="" id="main-form-x">
        <div class="row" style="margin-bottom: 25px;">
            @{
                int i = 0;
                foreach (var varik in Model.GetAllRapLines())
                {
                    i++;
                    <div class="form-check">
                        <label class="form-check-label" for="check-var-@i">
                            <input class="form-check-input" type="checkbox" value="@i" id="check-var-@i">
                            @varik.Value
                        </label>
                    </div>
                }
            }
        </div>
        <div class="form-group">
            <button type="button"
                    disabled="disabled"
                    id="xxxxx"
                    style="border: solid 5px cyan; width: auto; cursor:pointer; font-size: 20px; height: 100px;"
                    class="btn btn-primary btn-lg btn-block login-button">
                СКОПИРОВАТЬ РЕЗУЛЬТАТ
            </button>
        </div>
        <img src="~/Content/images/dollar-sign-symbol-24.gif" style="position: absolute; z-index: 10; right: 0; top: 0;" />
        <img src="~/Content/images/dollar-sign-symbol-24.gif" style="position: absolute; z-index: 10; right: 0;" />
    </form>
</div>